[project]
name = "sc2-matchmaking-prototype"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "requests",
]

[project.optional-dependencies]
dev = [
    "python-dotenv",
    "jupyter",
]
